import type { ModuleContent } from '../../types/content'
import { sobreMetadata } from './metadata'
import { sobreSections } from './sections'

export const sobreContent: ModuleContent = {
  metadata: sobreMetadata,
  sections: sobreSections,
}
